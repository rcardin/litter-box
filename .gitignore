logs/
.litter-box/logs/
.litter-box/.env
.scala-build/
.bsp/
.vscode/
.metals/

# The assembly the README tells every developer to build at the repo root
# (`scala-cli --power package . -o lb --assembly`). ~10 MB of untracked binary otherwise, and the
# loop itself runs `git add -A` before every gate pass, so untracked here means committed shortly.
/lb
/litter-box
/litter-box.jar
